import React from 'react';
import { Button } from 'react-bootstrap';
import Header from '../Header/Header';

const Main = () => {
    return (
        <div>

            <Header></Header>
            
        </div>
    );
};

export default Main;